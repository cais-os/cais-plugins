{
  "description": "Cais team hooks — enforces Conventional Commits",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-commit-message.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
